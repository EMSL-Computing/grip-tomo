# Copy this file to `.env`, adjust the values for your environment, then `source` it before running cisTEM workflow.

# Core directories
GRIPTOMO_OUTPUT_DIR=/absolute/path/to/output

# Executable overrides
GRIPTOMO_PYTHON_BIN=/absolute/path/to/.venv/bin/python
GRIPTOMO_MAX_WORKERS=4
IMOD_CTFPHASEFLIP_BIN=/absolute/path/to/ctfphaseflip
IMOD_TILT=/absolute/path/to/imod/bin/tilt
IMOD_TRIMVOL=/absolute/path/to/imod/bin/trimvol
IMOD_CLIP=/absolute/path/to/imod/bin/clip
RELION_IMAGE_HANDLER=/absolute/path/to/relion/bin/relion_image_handler
EMAN2_E2PROC2D=/absolute/path/to/eman2/bin/e2proc2d.py
CISTEM_BIN=/absolute/path/to/cistem/bin
CISTEM_HELPER_BIN=/absolute/path/to/.venv/bin/python
STAR_DIR=/absolute/path/to/stars
SLURM_SBATCH=/usr/bin/sbatch

# Safety toggles
FORCE=0  # set to 1 to allow scripts to overwrite existing outputs


# Optional module roots for convenience
# IMOD_HOME=/absolute/path/to/imod
# EMAN2_HOME=/absolute/path/to/eman2
# RELION_HOME=/absolute/path/to/relion

# Optional helper overrides (defaults resolve to bundled files under utils/)
# GRIPTOMO_TEMPLATE_ROOT=/custom/path/to/template_input
# GRIPTOMO_MERGE_SCRIPT=/custom/path/to/merge_mrc_s_in_this_list_noise.py
# GRIPTOMO_PHASE_TEMPLATE=/custom/path/to/in_nm.defocus_template
# GRIPTOMO_RECONSTRUCT_SCRIPT=/custom/path/to/2_reconstruct_simulated.bash
# GRIPTOMO_SINGLE_FOLDER_SCRIPT=/custom/path/to/at_single_folder.py
